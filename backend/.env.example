DATABASE_URL=postgresql://user:password@localhost:5432/dineops
JWT_SECRET=replace-with-a-strong-secret
# Optional on Railway: Railway sets PORT. Comma-separate multiple dev/staging origins if needed.
# FRONTEND_ORIGIN=https://your-frontend.vercel.app
# PORT=4000
#
# Render: DATABASE_URL is provided by Render Postgres; set FRONTEND_ORIGIN to your deployed SPA origin.
